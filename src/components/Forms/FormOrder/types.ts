export type UserFormData = {
  firstName: string;
  lastName: string;
  color: string;
  quantity: number;
  numberPhone: string;
  instagram: string;
};

export enum ColorType {
  PINK = "Рожевий",
  BlUE = "Синій",
}
