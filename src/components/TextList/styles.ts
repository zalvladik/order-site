import { List, ListItem, Typography, styled } from "@mui/material";

export const ListUl = styled(List)``;

export const ListItemLi = styled(ListItem)``;

export const TextListText = styled(Typography)`
  color: #fff;
  font-size: 18px;
  font-weight: 500;
`;
